# 已迁移：请从以下入口开始

- 主入口：`README.md`（项目概览与快速开始）
- 世界生成：`docs/WORLDPACK_QUICKSTART.md`
- 架构参考：`docs/architecture/ARCHITECTURE.md`

本页原内容涉及旧的 CLI/交互式小说生成流程，保留作历史参考。

### 方式 2: 手动激活环境

```bash
# 激活虚拟环境
source .venv/bin/activate

# 初始化数据库（首次运行）
python3 scripts/init_db.py

# 启动交互式生成器
python3 interactive_generator.py

# 退出虚拟环境
deactivate
```

### 方式 3: 使用 uv run（最快）

```bash
# 直接运行，uv 自动管理环境
uv run python3 scripts/init_db.py
uv run python3 interactive_generator.py
```

---

## 🎮 使用流程（历史版本）

### 1. 首次运行：初始化数据库

```bash
./run.sh scripts/init_db.py
```

预期输出：
```
============================================================
初始化 SQLite 数据库
============================================================
✅ 数据库表创建成功
```

### 2. 启动交互式生成器

```bash
./run.sh interactive_generator.py
```

### 3. 选择小说类型

```
选择小说类型:
1. 科幻小说 (能源纪元)
2. 玄幻小说 (逆天改命录)

请选择 (1/2): 1
```

### 4. 开始生成

系统会自动生成第一章，然后等待你的选择：

```
============================================================
第 1 章
============================================================

(AI 生成的 800-1200 字内容)

============================================================

你的选择:
>
```

你可以：
- **输入选择**：例如 "向导师汇报异常数据"
- **直接回车**：自动继续
- **输入 'save'**：查看统计
- **输入 'quit'**：退出

---

## 📊 数据存储

所有内容自动保存到：
- **数据库**: `./data/sqlite/novel.db`
- **导出小说**: `./output_novel_xxx.md`

---

## 💡 快速测试

### 测试 OpenRouter 连接

```bash
./run.sh test_openrouter.py
```

### 测试数据库

```bash
./run.sh test_database.py
```

---

## 🎯 完整启动命令（复制运行）

```bash
# 一次性完成：初始化数据库 + 启动生成器
./run.sh scripts/init_db.py && ./run.sh interactive_generator.py
```

---

## 📝 示例对话

```
$ ./run.sh interactive_generator.py

============================================================
 交互式小说生成器
============================================================

选择小说类型:
1. 科幻小说 (能源纪元)
2. 玄幻小说 (逆天改命录)

请选择 (1/2): 1

============================================================
开始创建小说: 能源纪元
============================================================
✅ 小说ID: novel_abc123
   类型: scifi
   主角: 林墨

📖 开始生成小说...

============================================================
正在生成第 1 章...
============================================================

(AI 生成第一章内容...)

你的选择:
> 向导师陈教授报告数据异常

✅ 已记录你的选择: 向导师陈教授报告数据异常

(继续生成第二章...)
```

---

## 🔧 故障排除

### 问题 1: 权限错误

```bash
chmod +x run.sh
chmod +x scripts/init_db.py
```

### 问题 2: 虚拟环境未激活

确保看到命令提示符前有 `(.venv)`：
```bash
source .venv/bin/activate
```

### 问题 3: 依赖未安装

```bash
uv pip install -r requirements.txt
```

---

## 🎉 现在就开始！

```bash
./run.sh interactive_generator.py
```

祝创作愉快! 📚✨
